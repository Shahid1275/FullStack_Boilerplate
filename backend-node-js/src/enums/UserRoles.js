/**
 * User Roles Enum
 * Application user roles
 */

const UserRoles = Object.freeze({
  SUPER_ADMIN: 'super_admin',
  ADMIN: 'admin',
  MODERATOR: 'moderator',
  USER: 'user',
  GUEST: 'guest'
});

module.exports = UserRoles;
