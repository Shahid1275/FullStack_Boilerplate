/**
 * Pipes Index
 * Data transformation pipes aggregator
 */

const ValidationPipe = require('./validation.pipe');
const TransformPipe = require('./transform.pipe');

module.exports = {
  ValidationPipe,
  TransformPipe
};
