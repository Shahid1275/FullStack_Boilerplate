/**
 * Shared Module Index
 * Shared utilities and common code aggregator
 */

const utils = require('./utils');
const dateHelpers = require('./date.helpers');

module.exports = {
  utils,
  dateHelpers
};
